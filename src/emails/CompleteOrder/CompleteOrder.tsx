import styles from './CompleteOrder.module.scss';
import type { CompleteOrderTypes } from './CompleteOrder.types';

const CompleteOrder = ({  }: CompleteOrderTypes) => {
  return (
    <section className={styles['CompleteOrder']}>

    </section>
  );
};

export default CompleteOrder;