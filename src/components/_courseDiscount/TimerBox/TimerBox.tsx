import styles from './TimerBox.module.scss';
import type { TimerBoxTypes } from './TimerBox.types';

const TimerBox = ({  }: TimerBoxTypes) => {
  return (
    <section className={styles['TimerBox']}>

    </section>
  );
};

export default TimerBox;