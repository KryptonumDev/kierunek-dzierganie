import styles from './ImageHeading.module.scss';
import type { ImageHeadingTypes } from './ImageHeading.types';

const ImageHeading = ({  }: ImageHeadingTypes) => {
  return (
    <section className={styles['ImageHeading']}>

    </section>
  );
};

export default ImageHeading;