import styles from './TimerText.module.scss';
import type { TimerTextTypes } from './TimerText.types';

const TimerText = ({  }: TimerTextTypes) => {
  return (
    <section className={styles['TimerText']}>

    </section>
  );
};

export default TimerText;