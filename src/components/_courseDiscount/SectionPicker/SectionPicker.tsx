import styles from './SectionPicker.module.scss';
import type { SectionPickerTypes } from './SectionPicker.types';

const SectionPicker = ({}: SectionPickerTypes) => {
  return <section className={styles['SectionPicker']}>hej</section>;
};

export default SectionPicker;
